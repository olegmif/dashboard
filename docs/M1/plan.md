# M1 — Каркас и окно-десктоп под dwm

**Цель:** доказать, что окно можно прибить к левому монитору как десктоп-виджет
под dwm, и заложить каркас приложения. Самый рискованный майлстоун — поэтому
первый. Закрывает разом весь стек: cgo, GTK, multi-head, дисциплину горутин.

> ⚠️ **Тулкит: сначала пробуем GTK4 (`diamondburned/gotk4`).** Если прибить окно
> к левому монитору под dwm на GTK4 окажется неподъёмно (GTK4 Wayland-first и
> прячет X11) — откатываемся на GTK3 (`gotk3`) и продолжаем M1 на нём.

## Фазы

### Ph1 — Пустое окно
- GTK4-окно без декораций (`SetDecorated(false)`).
- Запуск, чистое завершение.

### Ph2 — Геометрия левого монитора
- `GdkDisplay` → `GdkMonitor` по индексу; растянуть окно на геометрию монитора.
- Индекс левого монитора сверить с `xrandr --listmonitors`.

### Ph3 — «Прибить» окно под dwm
Выбрать опытным путём один из вариантов:
- **A. override-redirect** — dwm полностью игнорирует такие окна
  (`manage()` проверяет `override_redirect`). Ближе всего к conky.
- **B. правило dwm** — `WM_CLASS="dashboard"` → `isfloating=1`, нужный
  тег/монитор; окно undecorated + опускаем под остальные.
- Критерий: не тайлится, не ворует фокус, лежит под обычными окнами, остаётся
  на левом мониторе при переключении тегов/раскладок.

### Ph4 — Каркас приложения
- `config.toml` + загрузчик.
- `style.css` через `GtkCssProvider`.
- Layout под 5 зон (фото, часы, telegram, погода, иконки) — пустые заглушки.

### Ph5 — Дисциплина обновлений
- Демонстрационная горутина → канал → `glib.IdleAdd` обновляет тестовый label.
- Это эталон, по которому потом пишутся все виджеты.

## Критерий готовности
Окно стабильно висит на левом мониторе, не мешает работе на других мониторах,
каркас собирается, демо-обновление из горутины работает.

## Зависит от / влияет на
- Закрывает открытый вопрос «GTK4 (первый заход) vs откат на GTK3».
- Закрывает «одно окно или несколько» (фиксируем решение в progress.md).
